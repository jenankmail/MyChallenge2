import { createContext } from "react";

//** Please implement MyContext to solve the exercise */
export let MyContext =createContext({imageSize:"" });
export let PlaceContent=createContext({place:""});